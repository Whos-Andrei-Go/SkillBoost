specVersion: "v1"
serviceId: "skillboost"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "skillboost-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
